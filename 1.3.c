#include <stdio.h>

int main() {
    int x,y,prod;
    printf("Enter two numbers:");
    scanf("%d%d",&x,&y);
    prod=x*y;
    printf("The product of two numbers is: %d",prod);
    return 0;
    
}