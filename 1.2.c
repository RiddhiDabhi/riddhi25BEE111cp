#include <stdio.h>
int main() {
    int p,q,difference;
    printf("Enter two numbers:");
    scanf("%d%d",&p,&q);
    difference=p-q;
    printf("The difference of two numbers is: %d",difference);
    return 0;    
}